export enum Code {
  HTTP_SUCCESS = 200,
  HTTP_CREATED = 201,
  HTTP_ACCEPTED = 202,
  HTTP_CLIENT_ERROR = 400,
  HTTP_LOGIN_EXPIRE = 401,
  HTTP_AUTHENTICATE = 301,
  HTTP_FORBIDDEN = 403,
  HTTP_NOT_FOUND = 404,
  HTTP_SERVER_ERROR = 500,
  HTTP_BAD_GATEWAY = 502,
  HTTP_SERVICE_UNAVAILABLE = 503,
  HTTP_GATEWAY_TIMEOUT = 504,
}
